const numeros = [1, 2, 3, 4, 5, 6, 111, 12];

for (let i = 0; i < 6; i++) {
    console.log(i, numeros[i]);// Criando um laço for onde apresenta a posição e o mumero da posição da array 
}

//Da maneira que definimos o laço acima se acresentarmos mais um valor ele nçao sera apresentado 

const dezenas = [10, 20, 30, 40, 50, 60, 500, 600];


for (let a = 0; a < dezenas.length; a++) {
    console.log(dezenas[a]); // dessa maneira conseguimos percorrer todos os valores da array definindo 
}

// Acima  podemos verificar que idependente de quantos atributos inserirmos ele ira se repitir 